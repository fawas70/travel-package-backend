{
  "name": "travel-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "npx json-server --watch db.json --port $PORT",
    "dev": "npx json-server --watch db.json --port 5000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "JSON Server backend for travel app",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
